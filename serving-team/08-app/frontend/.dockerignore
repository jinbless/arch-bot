# 빌더가 npm ci로 재설치/재빌드 → 호스트 산출물은 컨텍스트에서 제외(용량·플랫폼 불일치 방지).
node_modules/
dist/
.env
.env.local
.env.*.local
.git/
# 만화 카드 WebP(~77MB) — 이미지에 굽지 않고 bind-mount로 서빙(chromadb·shared/reference 패턴).
# dev에서는 vite가 public/에서 직접 서빙하므로 로컬엔 있어야 하고, 이미지 컨텍스트에서만 뺀다.
public/cartoons/
