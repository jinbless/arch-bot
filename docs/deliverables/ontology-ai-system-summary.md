# 최종보고서 핵심 요약

원문: `최종보고서_온톨로지 기반 근로감독관 AI 지원시스템 구축 방안 연구_v1.pdf`  
작성 목적: 현재 `arch-bot` 프로젝트 설계의 기준 문서로 사용할 수 있도록, 보고서의 핵심 설계 사상과 구현 요소를 압축 정리한다.

## 1. 보고서의 문제의식

보고서는 기존 법령정보서비스의 한계를 단순한 검색 성능 문제가 아니라, 법령이 아직 “적용 가능한 규제지식”으로 구조화되지 못한 문제로 본다.

기존 방식의 한계는 다음과 같다.

- 키워드 검색은 관련 조문을 찾는 데는 유용하지만, 조문 간 위임·참조·예외·특례 관계를 충분히 추적하지 못한다.
- RDB 기반 법령 저장은 조문 구조와 이력 관리에는 강하지만, 규범의 의미와 판단 로직이 애플리케이션 코드나 사람의 해석에 흩어진다.
- 벡터 DB/RAG는 유사 문서 검색에는 강하지만, 시점별 유효 규범 선택, 예외·특례 적용, 상충 해결, 수치 검증을 안정적으로 수행하지 못한다.
- LLM은 자연어 설명에는 강하지만, 단독으로 법적 판단을 맡기면 할루시네이션, 시점 오류, 조문 누락, 일관성 부족 문제가 발생할 수 있다.

따라서 보고서는 법령을 단순 텍스트가 아니라 다음 요소가 결합된 지식체계로 보아야 한다고 제안한다.

- 문서 구조: 법령, 장, 절, 조, 항, 호, 목
- 규범 의미: 주체, 행위, 객체, 요건, 효과, 예외
- 관계 구조: 위계, 위임, 참조, 준용, 특례, 타법 연계
- 시간 효력: 시행일, 개정, 경과규정, 적용 시점
- 판단 로직: 우선순위, 상충 해결, 전방추론, 후방추론

## 2. 핵심 목표

보고서의 목표는 법령을 “찾는 시스템”이 아니라 “판단을 지원하는 시스템”으로 바꾸는 것이다.

핵심 목표는 다음 네 가지로 정리된다.

1. 법령의 정형 구조와 규범명제를 함께 모델링한다.
2. 법령 간 위임·참조·예외·특례 관계를 추적 가능한 링크로 만든다.
3. 시점별 적용 규범 선택과 규범 상충 해결을 규칙 기반으로 처리한다.
4. LLM은 판정자가 아니라 질의 해석, 용어 정규화, 설명 생성 보조자로 사용한다.

즉, 최종 판단의 근거는 온톨로지, 규칙, SPARQL, 검증 로직이 제공하고, LLM은 사람이 이해하기 쉬운 자연어 응답을 만드는 역할로 제한한다.

## 3. 법령 온톨로지의 이중 구조

보고서는 법령 온톨로지를 크게 두 축으로 설계한다.

### 3.1 정형적 체계 축

정형적 체계는 법령 문서의 물리적 구조를 표현한다.

예시는 다음과 같다.

```text
근로기준법
 └─ 제4장 근로시간과 휴식
     ├─ 제50조 근로시간
     ├─ 제53조 연장 근로의 제한
     ├─ 제56조 연장·야간 및 휴일 근로
     └─ 제63조 적용의 제외
```

이 축은 조문 원문, 조문 번호, 상하위 구조, 법령 버전, 개정 이력을 관리한다. 검색, 근거 추적, 개정 영향 분석의 기반이 된다.

### 3.2 규범명제 축

규범명제는 조문에서 실제 법적 판단에 쓰이는 최소 논리 단위다.

예를 들어 “사용자는 연장근로에 대하여 통상임금의 100분의 50 이상을 가산하여 지급하여야 한다”는 문장은 다음처럼 분해된다.

```text
주체: 사용자
행위: 지급한다
객체: 가산임금
요건: 연장근로가 발생한 경우
효과: 지급 의무 발생
양상: 의무
근거: 근로기준법 제56조
```

보고서는 이 규범명제를 `NormStatement` 같은 1급 객체로 모델링해야 한다고 본다. 그래야 특정 사실관계와 매칭해 “위반인지”, “의무가 발생하는지”, “예외가 있는지”를 계산할 수 있다.

## 4. 온톨로지 모델의 주요 레이어

보고서의 법령 온톨로지는 여러 레이어로 구성된다.

### 4.1 추상 클래스 레이어

공통 속성을 여러 구체 클래스에 반복해서 붙이지 않고, 공통 상위 추상 클래스로 묶는다.

주요 추상 클래스는 다음과 같다.

| 추상 클래스 | 의미 |
|---|---|
| `IdentifiedEntity` | 식별자를 가지는 객체 |
| `TitledEntity` | 제목 또는 표제를 가지는 객체 |
| `TextBearingEntity` | 원문 텍스트를 가지는 객체 |
| `OrderedEntity` | 정렬 순서를 가지는 객체 |
| `NumberedEntity` | 조문 번호 등을 가지는 객체 |
| `ReferenceableEntity` | 참조 대상이 될 수 있는 객체 |
| `VersionedEntity` | 버전 또는 유효기간을 가지는 객체 |
| `LinkEntity` | 관계 자체를 객체화한 링크 객체 |

이 설계의 이유는 `title`, `text`, `identifier` 같은 공통 속성의 `rdfs:domain`을 여러 클래스에 중복 선언할 때 발생하는 OWL/RDFS 의미론 문제를 피하기 위해서다. 여러 domain 선언은 OR가 아니라 AND처럼 추론되므로, 공통 속성은 상위 추상 클래스에 붙이는 편이 안전하다.

### 4.2 법령 위계 레이어

법령의 법체계상 위치를 표현한다.

예시는 다음과 같다.

```text
헌법
 └─ 법률
     └─ 대통령령
         └─ 총리령/부령
             └─ 규칙
```

보고서는 `NormativeLevel`과 `hierarchyRank`를 사용해 법령 단계의 순서를 모델링한다.

예를 들어:

```text
헌법 rank = 1
법률 rank = 2
대통령령 rank = 3
부령 rank = 4
```

이렇게 하면 상위법 우선 원칙을 rank 비교로 판단할 수 있다. 법령 간 관계는 단순 ObjectProperty 하나로 끝내지 않고 `NormativeHierarchyLink` 같은 링크 객체로 표현한다.

### 4.3 문서 구조 레이어

법령 본문의 구조를 표현한다.

주요 클래스는 다음과 같다.

| 클래스 | 의미 |
|---|---|
| `LegalInstrument` | 법령 문서 전체 |
| `StructuralUnit` | 조문 구조 단위의 추상 클래스 |
| `Part` | 편 |
| `Chapter` | 장 |
| `Section` | 절 |
| `Article` | 조 |
| `Paragraph` | 항 |
| `Item` | 호 |
| `Subitem` | 목 |
| `Appendix` | 부속 문서 |
| `Addendum` | 부칙 |
| `AnnexTable` | 별표 |
| `LegalForm` | 서식 |

이 레이어는 법령 원문을 트리 구조로 보존하고, 조문 단위의 버전 및 개정 이력을 추적하기 위한 기반이다.

### 4.4 규범명제 레이어

조문을 법적 판단 가능한 단위로 분해한다.

주요 클래스는 다음과 같다.

| 클래스 | 의미 |
|---|---|
| `NormStatement` | 규범명제 |
| `DeonticModality` | 의무·금지·허용·권한·면제 등 규범 양상 |
| `LegalRole` | 사용자, 근로자 등 법적 역할 |
| `LegalAction` | 지급, 금지, 신고 등 법적 행위 |
| `LegalObject` | 임금, 근로조건 등 행위의 대상 |
| `Condition` | 요건, 조건 |
| `LegalEffect` | 법적 효과 |
| `Sanction` | 벌칙, 제재 |
| `AuthorityAction` | 행정기관의 권한 행사 |

이 레이어의 핵심은 “조문 텍스트”와 “법적 판단 단위”를 분리하되, `expressedInUnit` 같은 관계로 근거 조문을 잃지 않게 하는 것이다.

### 4.5 관계/링크 레이어

보고서는 법령의 핵심 관계를 단순한 선이 아니라 별도 객체로 만든다. 이를 관계의 1급 객체화, 관계 객체화, Reification, Link Entity 패턴으로 설명할 수 있다.

주요 링크 클래스는 다음과 같다.

| 링크 클래스 | 의미 |
|---|---|
| `ReferenceLink` | 조문 간 인용, 참조, 준용 |
| `DelegationLink` | 위임 관계 |
| `InterLawLink` | 타법 연계 |
| `ModificationLink` | 예외, 특례, 준용, 적용 제외 |
| `CaseLawLink` | 판례 연결 |
| `ConstitutionalBasis` | 헌법적 근거 |
| `NormativeHierarchyLink` | 법령 간 위계 관계 |

관계를 객체로 만들면 다음 정보를 함께 저장할 수 있다.

- 관계 유형
- 근거 문구
- 적용 범위
- 연결 시작점과 대상
- 위임 또는 참조의 방향
- 추론이나 설명에 필요한 메타데이터

## 5. 시간효력과 버전 관리

보고서는 법령 판단에서 “어느 시점의 법이 적용되는가”가 매우 중요하다고 본다.

따라서 법령, 조문, 규범명제에 다음 정보를 붙일 수 있어야 한다.

- `validFrom`: 효력 시작일
- `validTo`: 효력 종료일
- `amendedOn`: 개정일
- `effectiveFrom`: 시행일
- `transitionCondition`: 경과규정 조건

예를 들어 근로시간 제한 규정은 개정 전후 내용이 다르고, 사업장 규모별 시행 시점도 다를 수 있다. 단순 RAG는 관련 조문을 모두 찾을 수는 있어도, 특정 사실관계 시점에 어떤 규범이 적용되는지 안정적으로 고르기 어렵다.

보고서는 이 문제를 온톨로지 속성과 SPARQL 질의, 규칙 기반 로직으로 해결해야 한다고 본다.

## 6. 상충 해결과 우선순위

법령에는 서로 다른 규범이 동시에 적용될 수 있다. 이때 최종 적용 규범을 고르기 위해 우선순위 원칙이 필요하다.

보고서가 강조하는 우선순위 원칙은 다음과 같다.

- 상위법 우선
- 특별법 우선
- 신법 우선
- 원칙보다 예외·특례 우선

예를 들어 일반 근로시간 규정이 있더라도, 적용 제외 조항이 특정 근로자에게 적용된다면 일반 규정보다 예외 규정이 우선한다.

이를 위해 보고서는 `NormConflictCase`, `ResolutionPrinciple`, `ResolutionDecision` 같은 객체를 둔다. 즉, 상충 상황과 해결 결과도 데이터로 남긴다.

## 7. 추론 구조

보고서는 법령 판단을 위해 전방추론과 후방추론을 모두 사용한다.

### 7.1 전방추론

전방추론은 입력된 사실에서 출발해 가능한 결론을 넓게 도출한다.

흐름은 다음과 같다.

```text
사실 입력
 → 규칙 매칭
 → 위반 여부 도출
 → 제재 가능성 도출
 → 감독 대상 여부 도출
```

예를 들어 사업장의 사실관계가 다음과 같다고 하자.

```text
서면 근로계약서 없음
주 근로시간 52시간 초과
연장근로 가산수당 부족
최저임금 미달
```

전방추론은 이 사실들로부터 여러 위반, 벌칙, 행정조치 가능성을 순차적으로 도출한다.

보고서의 구현 검증에서는 입력 사실 8건, 규칙 8개, 3개 라운드, 발화 규칙 6개를 통해 최종 결론 6건을 도출했다고 정리한다.

### 7.2 후방추론

후방추론은 목표 결론에서 출발해 그 결론이 성립하기 위한 요건을 역으로 확인한다.

흐름은 다음과 같다.

```text
목표 질문
 → 필요한 법적 요건 분해
 → 각 요건에 필요한 사실 확인
 → 충족, 미충족, 추가 확인 필요 판단
 → 최종 결론
```

예를 들어 “이 해고가 적법한가?”라는 질문이 있으면 다음을 역방향으로 검토한다.

- 정당한 이유가 있는가
- 해고 제한 기간에 해당하지 않는가
- 서면 통지가 있었는가
- 해고 예고 기간을 지켰는가
- 예외 사유가 있는가

보고서는 후방추론이 질의 중심 법적 판단, 상담, 적법성 점검에 적합하다고 본다.

## 8. SWRL, SPARQL, OWL의 역할

보고서는 여러 추론·검증 수단을 역할별로 나누어 사용한다.

| 기술 | 보고서에서의 역할 |
|---|---|
| OWL/RDFS | 클래스, 속성, 계층, domain/range, cardinality, disjointness 같은 기본 의미론 |
| SWRL/Jena Rules | IF-THEN 도메인 규칙 표현 |
| SPARQL CONSTRUCT/SELECT | 패턴 기반 트리플 생성, 질의, 물질화, 결과 조회 |
| Reasoner | 타입 전파, 상속 추론, 일관성 검사 |
| SHACL | 제약 검증에 활용 가능한 표준 기술로 언급 |
| Python/Rule Engine | 실무 구현에서는 패턴 추출, 검증, 규칙 실행에 활용 가능 |

보고서의 중요한 설계 방향은 “LLM이 판단하는 것”이 아니라 “온톨로지와 규칙이 판단하고 LLM이 설명한다”는 역할 분담이다.

## 9. LawBrain 아키텍처

보고서의 AI 법령서비스 구현체는 `LawBrain`으로 제시된다.

LawBrain의 응답 구조는 다음 네 가지 섹션으로 구성된다.

1. 핵심 결론
2. 근거 법령
3. 상세 설명
4. 연관 정보

시스템 계층은 다음과 같다.

```text
사용자 인터페이스
 → 질의 분해 및 온톨로지 매핑
 → 추론 엔진
 → RDF/OWL 지식 기반
 → LLM 설명 생성
```

자연어 질의는 법적 의미 단위로 분해되고, 온톨로지 개념 계층을 통해 의미 확장된다. 이후 SPARQL 쿼리로 조문 제목, 조문 본문, 참조 텍스트, 위임 범위, 타법 연결, 법률명 등을 Tier별로 탐색한다.

보고서는 LawBrain이 18단계 추론 파이프라인을 통해 단순 검색보다 더 구조적인 답변을 생성한다고 설명한다.

## 10. 비교 평가 결과

보고서는 온톨로지 기반 RAG와 하이브리드 서치 기반 RAG를 비교한다.

평가 질의는 다음과 같은 유형이다.

- 장시간 근로 후 추가 수당 청구 가능 여부
- 벌칙 조문이 처벌하는 대상 추적
- 출산휴가 미부여 시 제재
- 강제 야근의 위법성
- 임의 해고의 적법성
- 고등학생 아르바이트 가능 여부
- 미성년자 야근의 위법성

정량 결과는 다음처럼 제시된다.

| 평가 지표 | 온톨로지 기반 RAG | 하이브리드 서치 RAG |
|---|---:|---:|
| 평균 Precision | 98.6% | 96.4% |
| 평균 Recall | 98.8% | 40.2% |
| 관계 추적 성공률 | 100% | 0% |
| 평균 근거 투명성 | 5.0 / 5.0 | 2.6 / 5.0 |
| 환각 발생률 | 0% | 28.6% |
| 평균 검색 조문 수 | 6.3건 | 2.4건 |

보고서는 특히 Recall 차이를 중요하게 본다. 법령 서비스에서는 “그럴듯한 조문 몇 개”보다 “빠뜨리지 않고 관련 조문을 추적하는 것”이 중요하기 때문이다.

## 11. 보고서의 결론

보고서의 결론은 다음과 같이 압축할 수 있다.

법령 온톨로지는 단순한 지식 표현 도구가 아니라, LLM의 추론 품질을 구조적으로 보완하는 핵심 인프라다. 법령을 조문 텍스트가 아니라 규범명제, 위임, 참조, 예외, 시간효력, 제재, 법령 위계가 결합된 지식그래프로 만들면, 검색을 넘어 판단 지원이 가능해진다.

특히 실무 시스템에서는 다음 구조가 바람직하다.

```text
전방추론: 가능한 위반·제재·행정조치를 넓게 탐색
후방추론: 특정 쟁점의 요건 충족 여부를 깊게 검증
LLM: 질의 해석, 용어 정규화, 설명 생성
온톨로지/규칙/SPARQL: 핵심 판단과 근거 추적
```

## 12. 한계와 향후 과제

보고서가 제시하는 한계는 다음과 같다.

- 검증 범위가 근로기준법 중심이므로 다른 법령군에 대한 일반화 검증이 필요하다.
- 규범명제 추출에서 예외, 특례, 중대성 판단 등은 여전히 전문가 검토가 필요하다.
- 정량 평가는 기능 검증 중심이므로 전문가 정답셋 기반 평가가 더 필요하다.
- 대규모 법령 전체를 대상으로 ABox를 자동 구축하고 유지보수하는 파이프라인이 필요하다.
- 온톨로지 기반 RAG는 정확성과 투명성은 높지만 응답 시간 최적화가 필요하다.

향후 과제는 다음과 같다.

- 산업안전보건법, 고용보험법, 남녀고용평등법 등으로 법령 범위 확장
- 정밀도, 재현율, F1, 검토 시간 단축률, 누락 감소율 등 평가체계 고도화
- 법령 개정 시 규범명제, 링크, 시간효력 자동 갱신
- 전방추론과 후방추론을 결합한 통합 추론 워크플로 구현
- 비정형 사용자 사실관계를 ABox 인스턴스로 자동 정규화
- SPARQL 쿼리 최적화, 캐싱, 인덱싱 등 성능 개선
- 감독, 점검, 상담, 민원 응대에 맞춘 UI와 업무 기능 확장

## 13. arch-bot 프로젝트 설계로 이어지는 핵심 포인트

현재 `arch-bot` 프로젝트가 이 보고서를 기준으로 설계되었다면, 핵심 설계 기준은 다음과 같이 볼 수 있다.

1. OWL 파일은 단순 데이터 파일이 아니라 TBox, 즉 법령·지침 세계의 개념 구조와 관계 규칙을 정의하는 스키마다.
2. TTL 인스턴스 파일은 ABox, 즉 실제 조문, KOSHA Guide, 규범명제, 체크리스트, 안전요건 등을 담는 데이터 그래프다.
3. `NormStatement`나 `SafetyRequirement` 같은 단위는 조문이나 지침 원문을 실제 판단 가능한 단위로 바꾼 것이다.
4. `ReferenceLink`, `DelegationLink`, `ModificationLink`, `NormativeHierarchyLink` 같은 링크 계열은 관계를 단순 선이 아니라 근거와 범위를 가진 객체로 다루기 위한 설계다.
5. `TitledEntity`, `TextBearingEntity`, `VersionedEntity` 같은 추상 클래스는 공통 속성의 domain 충돌을 피하고, 속성 의미를 일관되게 유지하기 위한 설계다.
6. SWRL은 규칙 설계 문서 또는 추론 규칙 표현으로 볼 수 있고, 실제 운영에서는 성능과 통제성을 위해 SPARQL CONSTRUCT나 Python rule engine으로 물질화할 수 있다.
7. LLM은 최종 판정자가 아니라, 온톨로지와 규칙이 산출한 근거를 사람이 이해할 수 있게 설명하는 계층으로 두는 것이 보고서의 핵심 방향이다.

## 14. 한 문장 요약

이 보고서의 핵심은 “법령을 검색 가능한 텍스트가 아니라, 조문 구조·규범명제·관계·시간효력·추론규칙이 결합된 적용 가능한 지식그래프로 만들고, LLM은 그 결과를 설명하는 역할로 제한하자”는 것이다.
