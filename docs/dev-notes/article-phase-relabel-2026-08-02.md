# 조문 시점 재판정: 제목이 아니라 원문으로 (2026-08-02)

앞 세션 정본은 [앵커→흐름 서빙](anchor-flow-serving-2026-08-01.md)이다.
이 문서는 그 다음 작업 — **흐름 6칸의 시점 라벨을 다시 만든 것**을 남긴다.

## 커밋 17개 (main, push 안 함)

| 커밋 | 내용 |
|---|---|
| `8417169` | 적용범위 자기한정 조문 상속 차단 — **같은 버그 6번째** |
| `ba73524` | 조문 시점을 제목이 아니라 원문으로 판정 — EXEC 96% 편중 해소 |
| `048ee6b` | 안전검사 scope·cycle을 법령 원문과 글자 단위 대조 |
| `7c45886` | 안전검사 주기 오배정 + 적용 범위 미노출 — **버그 7번째** |
| `551bce2` | 법·시행령·시행규칙 554조 훑기 — 흐름에 빠진 의무 43건 |
| `2e2758e` | 법령 의무 배선 40/43건 |
| `efaae8d` | 시행규칙 별표 5 특별교육 대상작업 39종 확보 |
| `2328dc2` | 특별교육 배선 — **43/43 완료** |
| `05dc44e` | 검수 뷰어 우선순위 |
| `a9ed12b` | 제38·39조를 적용 대상에만 — **같은 버그 8번째, 가장 컸다** |
| `cfa43cd` | 사진 앵커 적격 분류 — 127종 중 34종은 지목 불가 |
| `ad13bb9` | 제52조 계획 칸에서 기각 — **사람 검수가 3인 다수결을 뒤집었다** |
| `7fce25c` | 비계를 앵커 카탈로그로 — **앵커 0.711 → 0.600** |
| `84013ac` | '기본 안전수칙' 카테고리 47건 |

---

## 1. 무엇이 잘못돼 있었나

`build_flow_slice_all.py`의 `phase_of()`가 조문 **제목**만 정규식으로 봤다.

```
669개 조문 → EXEC 642(96.0%) · POST 14 · PRECHECK 7 · ASSIGN 4 · PLAN 2 · PERIODIC 0
```

제89조는 본문에 "그 날의 작업을 시작하기 전에"가 있는데 제목이 '운전 시작 전 조치'라
정규식(`시작하기 전`)에 안 걸려 EXEC로 갔다. **타임라인이라 부르지만 한 덩어리였다.**

원문(`data-team/02-extraction/pipe-A/data/article-texts.json`, RULE 690조 fullText)이
이미 있는데 안 쓰고 있었다.

## 2. 어떻게 판정했나 — 양방향

LLM이 판정하되 **혼자 결정하지 못하게** 한다. 산출물은 `article_phase_llm/`에 원본 그대로 남아
`build_article_phases.py`를 다시 돌리면 같은 결과가 나온다.

| 단계 | 하는 일 | 결과 |
|---|---|---|
| 1차 | 원문 판독 + **원문 인용**(12배치) | 807 주장 |
| 2차 | 비-EXEC 주장을 심판이 반박 | 27 반박 |
| 3차 | 반박된 것만 재심 | 19 기각 · 8 부활 |
| 역방향 | EXEC 단독 504조에서 **놓친 시점** 탐색(8배치) | 37 주장 |
| 역검증 | 새 주장을 심판이 반박 | 14 반박 → 23 통과 |
| 보완 | 작업 중이 빠진 40조 · 제목 같은데 갈린 5묶음 | 11 추가 |
| 최종 | 파이프라인이 **자기와 충돌한** 8건을 3인 다수결 | 8 채택 |
| 대조 | 인용문이 원문에 글자 그대로 있는지 (결정론) | **실패 0건** |

### ★ 역방향이 없으면 편향된 래칫이 된다

1차에 "EXEC가 안전한 기본값, 애매하면 EXEC"라고 지시했다. 그러면 2·3차는
**비-EXEC를 EXEC로 깎기만 하고 EXEC에서 꺼내는 경로가 없다.**
실제로 2차 반박 27건이 **27건 모두** 결론이 EXEC였다. 검증이 아니라 한 방향 래칫이었다.

이걸 워크플로의 비판 에이전트가 잡아냈고, 반대 방향을 따로 돌려서 23건을 되살렸다.
그중 제388조 「사용 전 점검 등」은 **제목에 '사용 전'이 있는데도** EXEC 단독이었고,
같은 제목의 제441·456·553·612조는 전부 PRECHECK였다(배치 간 불일치).

### 파이프라인이 자기와 충돌한 8건

1차 찬성 → 2·3차 반대 → 역방향 찬성 → 역검증 찬성 = **2:2**.
조용히 한쪽으로 기울면 어느 쪽이 죽었는지 아무도 모른다.
관점이 다른 3인(문언 / 의무이행 / 오안내)이 원문을 직접 읽고 다수결로 정했다.

| | 표 | |
|---|---|---|
| 제40조/PLAN · 제141조/PLAN · 제406조/PLAN · 제436조/PLAN · 제517조/PLAN | 3:0 | 만장일치 채택 |
| 제52조/PLAN · 제134조/PRECHECK · 제338조/PRECHECK | 2:1 | 채택(**반대의견 `tiebreak.json`에 보존**) |

3인이 갈린 3건은 **사람 검수에서 먼저 볼 것.** 예: 제338조의 반대의견 —
"'위험을 미리 방지하기 위하여'의 '미리'는 '방지'를 꾸미는 목적구이지 점검 시점이 아니다."

## 3. 결과

### 칸별 조문 수 (한 조문이 여러 칸에 걸칠 수 있다 — 다중 129종)

| 칸 | 제목 정규식 | **원문 판독** |
|---|---:|---:|
| 계획·사전조사 | 2 | **32** |
| 인적 배치·자격 | 4 | **50** |
| 작업 시작 전 점검 | 7 | **58** |
| 작업 중 | 642 | 635 |
| 종료·이탈 | 14 | **29** |
| 정기점검 | 0 | **19** |

### 흐름 실질 채움 (총칙 공통주입 제외)

| 칸 | 이전 | **이후** |
|---|---:|---:|
| 계획 | 13% | **31%** |
| 인적 | 17% | **46%** |
| 작업 전 | 20% | **49%** |
| 작업 중 | 99% | 90% |
| 종료 | 25% | **35%** |
| 정기 | 10% | **25%** |

**실질 1칸짜리 68종 → 29종.** 항목 1,962 → 2,210.
단 **실질 0칸이 13종 생겼다**(아래 §5).

## 4. 화면에 근거를 같이 보여준다

`FlowItem.evidence` = 그 항목을 그 칸에 넣은 **조문 원문 문구**.

- 같은 조문이 두 칸에 뜨는 일이 정상이 됐다(제35조 = 인적 배치 + 작업 전 점검).
  제목만 보면 중복으로 읽히므로 근거가 있어야 다른 의무임을 안다.
- ★ **1차의 EXEC 인용은 근거가 아니라 자리표시다.** "시점 문구가 없으면 EXEC 하나만 내고
  quote에는 핵심 의무 문장을 넣어라"고 지시했기 때문이다. 그대로 띄우면 제388조처럼
  **한 문장이 '작업 전'과 '작업 중'에 나란히** 떠서 중복으로 보인다(19종에서 발생).
  → `quote`(감사용)와 `evidence`(표시용)를 분리했다. 항목 자체는 빼지 않는다 —
  제620조("작업을 시작하기 **전과 작업 중에**")처럼 한 문장이 정말 두 시점을 다 말하는 경우가 섞여 있다.

## 5. 곁가지로 잡은 결함 4개

**① 정의·목적·적용제외 조문 30종이 '작업 중' 칸에 떠 있었다** — 제1조(목적), 제2조(정의)까지.
`no_duty`로 표시해 흐름에서 뺐다(38건 노출 제거).
→ 그 결과 **통칙 13종이 빈 흐름**이 됐다(편3 보건 각 장 통칙 등, 조문이 정의뿐).
   빈 사유를 따로 쓴다: "정의·적용범위 조문(제N조)으로만 이루어져 있어 그 자체로는 할 일이 없습니다".

**② 조문 정기 의무가 '안전검사(법정)'로 집계**돼 대상 12종이 29종으로 부풀었다.
`n_periodic_law = slots["PERIODIC"]`가 조문분까지 셌다. 예전엔 조문이 정기 칸에 오는 일이
없어(제목 정규식이 하나도 못 잡았다) 잠복해 있던 버그다.
→ `periodic_source`를 조문/안전검사/가이드 합성 문자열로 바꿨다.

**③ 검수 뷰어의 법정/권고 분기가 반대였다** — `source.indexOf('법정')>=0 ? law : rec`이라
새로 들어온 `조문(전용)`이 **권고**로 표시됐다. 규칙이 직접 정한 정기 의무를 KOSHA 권고로
띄우는 것은 반대 방향의 오안내다. 백엔드와 같은 `'권고' 유무` 기준으로 맞췄다.

**④ 에이전트가 `add07.json`을 안 썼다** — StructuredOutput은 냈는데 파일 쓰기를 건너뛰어
4건이 조용히 사라질 뻔했다. 워크플로 트랜스크립트에서 복구했고,
`read_all`이 `{"additions":[…]}`뿐 아니라 배열 형식도 받도록 고쳤다(다른 에이전트는 배열로 썼다).
→ **파일과 StructuredOutput이 갈릴 수 있다.** 파일을 읽는 쪽이 개수를 대조해야 한다.

## 6. ★ 적용범위 자기한정 — 같은 버그 6번째 (`8417169`)

조문 본문이 스스로 적용 대상을 한정하는데 화이트리스트에 없어 엉뚱한 기인물에 붙고 있었다.

| 조문 | 어디에 잘못 붙었나 | 근거 |
|---|---|---|
| 제98조 제한속도의 지정 | 프레스·공작기계·보일러·산업용 로봇 등 **16종** | ①차량계 하역운반기계·건설기계 ②궤도작업차량·입환기 한정 |
| 제86조 탑승의 제한 | 건설기계 3종 포함 **11종** | 각 항이 양중기·차량계 하역운반기계·컨베이어를 열거. 건설기계는 없다 |
| 제133조 정격하중 표시 | 승강기 | "양중기(**승강기는 제외한다**)" |
| 제178조 허용하중 제한 | 고소작업대 | ①지게차 ②구내운반차·화물자동차 |

- 차량계 건설기계 탑승금지는 **제202조**가 따로 규율하므로 제86조를 빼도 구멍이 안 생긴다.
- 제98조는 궤도 보수·점검(편2장8절2), 입환작업(편2장8절3) 2종에 **신규 주입**했다.
- **절 총칙(관1) 상속 경로에는 적용범위 검사가 아예 없었다.** 절 총칙이라고 절 전체에 다 걸리지 않는다.
- `SCOPED` → `SCOPE`로 바꾸고 allow/deny + **좌표 접두사** 비교로 통일. 상속 세 경로가 같은 `applies()`를 쓴다.
- 기계 일반기준 절1 **14개 조문 본문을 전수 확인**했다. 자기한정은 제86·98·99조 셋뿐이고
  제87·88·90~97조는 "기계"·"동력으로 작동되는 기계"라 진짜 일반이다.

⚠ 제41조는 **방호장치가 아니라 '운전위치의 이탈금지'**다(주석이 틀려 있었다). 이번에 고쳤다.
   그리고 1차 판정이 이 조문을 POST로 보냈는데 심판이 반박했다 —
   **이탈 시 조치가 아니라 이탈 금지**라 작업 중 의무다.

## 7. 안전검사 scope·cycle 법령 대조 (`048ee6b`)

**시행령 제78조·시행규칙 제124~126조·법 제93조는 밖에서 받아올 것이 아니라
`article-texts.json`에 처음부터 들어 있었다**(DECREE 124조 / ENFORCE 246조).
"미확보"라고 적어 둔 것이 틀렸다. 다음부터 **없다고 적기 전에 있는 파일부터 뒤질 것.**

`verify_against_law.py` — 눈으로 옮겨 적지 않고 스크립트로 대조한다. 검사 6종:
이름 · scope · source_ref(호 번호) · cycle 조각의 부분문자열 여부 · 역방향 누락 ·
**단서 오배정**(호의 괄호가 특정 기계를 지목하면 그 기계에만 `special`이 붙어야 한다).

| | |
|---|---|
| 일치 | **67건** |
| 불일치 | **1건** — 사출성형기 `294킬로뉴턴(kN)` → 원문은 `(KN)`. 22번째 글자 |

→ `--write`가 **scope를 시행령 원문에서 직접 잘라 넣는다.** 전사 단계를 없애면 이 부류의 오류가
원리적으로 생기지 않는다. 재실행하면 불일치 0. cycle에는 호 전문을 `verbatim`으로 함께 남겼다.

### 혼합기·파쇄기 적용 제외 범위 — **시행령에 없다**

제14호 '혼합기', 제15호 '파쇄기 또는 분쇄기' 둘 다 괄호 단서가 없다.
같은 조 제2항이 "세부적인 종류, 규격 및 형식은 고용노동부장관이 정하여 고시한다"고 위임하는데,
확보한 「안전검사 고시」는 **검사기준만** 정한다(제1~33조 전문 확인).
→ 위임받은 별도 고시(「안전검사대상기계등의 규격 및 형식별 적용범위」)에 있을 것으로 보이나 미확보.
   **추정하지 않고 비워 둔다.**

### 대조하다 드러난 공백 (`verification.알려진_공백`)

- **시행규칙 제125조 면제 사유 11개가 미반영이다** — 건설기계관리법·고압가스법·전기안전관리법·
  위험물안전관리법 등 다른 법령 검사를 받으면 안전검사가 면제된다.
  화면이 '법정 — N년마다'만 보여주면 **면제 가능성이 통째로 숨는다.**
- 시행규칙 제124조(주기 만료일 30일 전 신청, 만료일 전후 30일 이내 검사) 미반영

흐름 산출물은 재생성해도 **바이트 동일** — `scope`·`verbatim`은 흐름에 쓰이지 않는다.

## 8. 재료 범위를 규칙 → 법 전체로 넓혔다 (`551bce2` → `05dc44e`)

흐름 6칸의 재료가 **산업안전보건기준규칙(669조)과 그 별표 2·3·4, 안전검사 고시뿐**이었다.
법·시행령·시행규칙 554조를 훑어 기인물 단위로 매달 수 있는 의무 **43건**을 찾아 전부 붙였다.

### 붙이는 방식을 표로 명시한다 (`law3_targets.py`)

코드가 대상을 알아서 정하게 두면 과부착한다. 조문마다 대상과 근거를 적었다.

| kind | 뜻 | 쓰인 곳 |
|---|---|---|
| `machines` | 법령이 정한 **닫힌 기계 목록** → 그룹 | 영 별표 20·21, 영 제66·74·77조 |
| `coord` | 좌표 접두사 | 석면 (3,2,6) · 관리대상 (3,1) · 이상기압 (3,5) · 비계 (1,7) |
| `all_machine` | 편2장1 전체 | 법 제140조(대상 목록 미확보) |
| `byeolpyo5` | 특별교육 대상작업 39종 → 그룹 매핑표 | 법 제29조, 규칙 제26·27조 |

### 새로 파싱한 별표 (시각 판독)

`data-team/01-parsing/law-appendices/` — 시행령 별표 20(방호조치 6종)·별표 21(대여 25종),
시행규칙 별표 4(교육시간 26행)·**별표 5(교육내용 81행, 특별교육 대상작업 39종)**.

⚠ **별표 4·5의 검산 에이전트가 `pdftotext`를 썼다** — 이 프로젝트가 금지하는 방법이다(한글 공백 소실).
  스스로 "육안 재확인은 하지 않았다"고 기록했다. 사람이 제1~6호·제36~39호를 렌더 이미지로 직접
  대조해 일치를 확인했고(붙여 쓴 '관한사항'까지 그대로), 나머지 29종 미대조는 manifest에 남겼다.
  검산이 전사 오기 2건을 잡기는 했다(제4호 '인화점 **등**', 제18호 '**공통**작업 신호').

### 특별교육 39종 → 기인물 매핑

LLM 매핑 → 독립 심판 반박(1건 기각) → **그룹키 실재 검산(59/59 유효)** → 사람이 맥락 확인.
36/39종이 붙었고 **3종은 대응 그룹이 없다**(집재장치·주물단조·목조건축물).

작업 **자체**를 항목으로 넣는다 — 조문(법 제29조)만 넣으면 '이 기인물의 **어떤 작업**이
특별교육 대상인가'가 안 보인다. 크레인 인적 배치 칸에 세 작업이 나란히 뜬다.

### 결과

| | 이전 | **이후** |
|---|---:|---:|
| 검수 항목 | 2,225 | **2,658** |
| 실질 계획 | 31% | **40%** |
| 실질 인적 | 46% | **66%** |
| 실질 작업 중 | 90% | **93%** |
| 실질 0칸 | 13종 | **9종** |

석면 그룹이 계획→인적→작업중→종료 네 칸을 실제로 채운다
(조사 → 해체업체 자격·교육 → 작업기준 → 완료 후 농도).

### 이번에도 좌표에서 실수했다 (8번째가 될 뻔했다)

`law3_targets.py`의 좌표를 **눈대중으로 적었더니 전부 틀렸다** — 석면 (3,1,2)→**(3,2,6)**,
화학설비 (2,1,4)→**(2,2,4)**, 비계 (2,4,1)→**(1,7)**. 산출물과 대조해 고쳤다.
그리고 규칙 별표 6 경유가 **조용히 실패해 72건이 안 붙고 있었다**(괄호 뗀 이름을 후보에 안 넣어서).

### 법령이 대상으로 정했는데 기인물 그룹이 없는 기계

예초기 · 공기압축기 · 금속절단기 · 포장기계 · 파워 셔블 · 클램셸 · 트렌치 · 천공기 · 롤러기 ·
사무실 및 공장용 건축물. **억지로 붙이지 않고 목록으로 남긴다.**

## 9. 검수 뷰어에 우선순위 (`05dc44e`)

2,658건을 순서대로 볼 수는 없다. 틀렸을 가능성이 높은 것부터 본다.

| | 건수 | |
|---|---:|---|
| ① 판정이 3인 중 2:1로 갈림 | 22 | 제52·134·338조 — **사람이 정해야 한다** |
| ② 이름 매칭 | 94 | 좌표 매칭보다 불확실 |
| ③ 법령 — 이번에 새로 붙임 | 433 | |
| ④ 규칙 조문 | 1,721 | 좌표 매칭 |
| ⑤ 별표·안전검사·가이드 | 388 | |

'의심스러운 것만' 버튼이 ①② **116건**만 남긴다. 목록의 ⚑는 우선 검수 항목이 있는 그룹(21종).
갈린 조문은 `tiebreak.json`의 표에서 자동으로 잡는다.

## 10. ★ 제38·39조를 적용 대상에만 — 같은 버그 8번째, 가장 컸다 (`a9ed12b`)

사용자 지적: "제38조는 조문에 명시된 작업을 할 때 별표 4에 따라 작업계획서를 쓰는 건데
왜 추락·붕괴·비계 같은 관계 없어 보이는 곳에 다 들어가 있지?"

제38조제1항은 **13개 작업만 열거하는 닫힌 목록**이다(별표 4와 1:1). 그런데
"전 기인물 공통 주입"이라며 126개 그룹에 무조건 넣고 있었다.

| 조문 | 붙어 있던 그룹 | 원문이 정한 대상 |
|---|---:|---:|
| 제38조 사전조사·작업계획서 | 126 | **10** |
| 제39조 작업지휘자 지정 | 126 | **5** (제38조 제2·6·8·10·11호 + 항타기·항발기) |
| 제35조 관리감독자 직무·점검 | 126 | 별표 2·3이 정하는 작업뿐 |

**계획·인적·작업전 칸이 '겉보기 100%'였던 것이 순전히 이 주입 때문이었다.**
'겉보기 vs 실질' 두 수치를 나눠 내던 것도 이 왜곡을 덮으려던 장치였다 — 이제 하나만 낸다.
제35조는 아예 뺐다(별표 2·3 항목이 이미 `제35조제1항/제2항`을 ref로 달고 있다).

항목 2,658 → **2,295**. 채움률: 계획 39% · 인적 67% · 작업전 49% · 작업중 93% · 종료 35% · 정기 25%.
빈 흐름 9종, 1칸 16종 — 얇다는 사실이 이제 숫자에 드러난다.

## 11. 사진 앵커 적격 분류 (`cfa43cd`)

사용자 지적: "통칙>통칙 / 보호구>보호구 / 관리감독자의 직무 — 흐름을 정의할 필요가 있나?"

앵커 카탈로그 127종은 규칙의 절·관 구조를 그대로 옮긴 것이라 **사진으로 지목할 수 없는 칸**이 섞여 있다.

  기인물 52 · 장소 22 · 환경 19 · **부적격 34**

부적격 판정만 심판이 뒤집기를 시도해 7건을 되살렸다(피뢰설비·휴게시설·지하작업장·분진 청소·
사무실 결로·환기장치·유해인자 3분야의 유일 앵커).
⚠ 되살린 7건의 kind를 스크립트가 전부 '기인물'로 뭉개고 사유는 옛 부적격 근거를 남겼다 — 정정했다.

**지우지 않고 표시한다.** `anchor_kind`/`anchor_why` → 화면이 "사진에서 지목할 수 있는 기인물이
아닙니다 … 목록에서 직접 찾기"를 띄우고, 선택기에서 뒤로 밀며 '사진 지목 불가' 배지를 단다.

## 12. ★ 제52조 — 사람 검수가 3인 다수결을 뒤집었다 (`ad13bb9`)

사용자 검수: "제52조는 신규건축공사면 무조건 해야 하는건가?" → **아니다.**

본문이 "구축물등이 **다음 각 호의 어느 하나에 해당하는 경우에는**"으로 시작하는 **조건부 발동 의무**다.
각 호 대부분이 이미 발생한 상태를 요건으로 한다(제1호 '발생하여', 제2호 '발생했을 경우').
'미리 제거'의 '미리'는 위험이 현실화하기 전이지 작업 착수 전이 아니다.
착수 전 기본 의무는 **제51조**(설계도서 준수)이고 제52조는 이상 상황에서 발동한다.

3인 판정은 2:1로 채택했다. **문언 관점 판정자의 반대의견이 옳았다.**
→ `article_phase_llm/human_review.json` 신규. **사람 검수는 다른 모든 판정을 덮어쓴다.**

★ 갈린 3건 중 **첫 번째에서 다수결이 틀렸다.** 나머지 둘(제134·338조)도 같은 눈으로 볼 것.
  이 건은 6칸이 담지 못하는 **트리거 발동 의무** 유형이기도 하다.

## 13. 비계를 앵커 카탈로그로 — 0.711은 비계 사진 15장을 안 세던 값 (`7fce25c`)

**비계는 앵커로 도달할 경로가 아예 없었다.** `cross_cutting`(= RESOLVE 카탈로그 제외 목록)에
'장7 비계'가 통째로 들어 있어 6종이 빠졌고, 항상-후보인 `CROSS` 16개 조문에도 비계는 없다.
RESOLVE의 자유 텍스트는 '비계(발판)'이라고 정확히 말하고 있었는데 group_key로 옮기지 못했다.

⚠ `cross_cutting`의 효과는 **하나뿐이다 — 카탈로그에서 제외.** 조문 후보의 '항상 포함'은
  `cue_article_service.CROSS`의 16개 조문이 따로 담당한다. 앞서 "횡단은 항상 후보에 들어간다"고
  적었던 것은 부정확했다.

측정도 같이 틀려 있었다 — `편1이면 정답에서 제외`가 하드코딩돼 정답이 비계뿐인 사진 15장이
**채점 자체에서 빠져 있었다.** 정답 필터를 **카탈로그에서 역산**하도록 바꿨다.

| 같은 60장 | 관 일치 | 완전 오인식 |
|---|---|---|
| 옛 카탈로그(99종)+옛 캐시 | 0.533 | 45.0% |
| **새 카탈로그(105종)+재실행** | **0.600** | **38.3%** |

정본 **0.711 → 0.600**(채점 45 → 60장). 0.778→0.711은 좌표 버그, 0.711→0.600은 채점 범위.
`rerun_resolve_cache.py` 신규(원본 캐시 보존, v2에 기록). 129장 중 39장이 비계를 지목하기 시작.
LLM이 카탈로그 줄 전체(`… ::기인물=… (N조)`)를 복사하는 경우 5장 — `_norm_gk()`로 정리.

## 14. '기본 안전수칙' 카테고리 (`84013ac`)

흐름 6칸은 앵커에 매달리는데, **시간축이 없고 현장이 늘 갖춰야 할 상태**인 의무는
앵커에 안 걸리면 볼 방법이 없다. 별도 화면으로 뺐다.

  작업장 시설 19 · 통로·계단 10 · 추락·붕괴 방지 12 · 보호구 4 · 특수형태근로종사자 2 = **47건**

담는 기준 셋을 **모두** 만족해야 한다: ① 앵커 카탈로그 밖 ② 다른 흐름에 이미 없음 ③ 기인물이 아님.
②가 결정적이다 — 후보 315건 중 **252건이 중복**이었다(양중기 총칙·차량계 총칙·기계 일반기준은
각 기인물 흐름에 상속된다). ③ 때문에 곤돌라를 뺐다.
★ 조문 단위로 합친다 — 흐름과 달리 시간축이 없어 그대로 펼치면 제15·42·44조가 두 번 뜬다(53 → 47).

`GET /api/v1/flow/always` · `/basics` 라우트 · 상단 메뉴 '기본 안전수칙'.
화면이 **"기인물을 잘못 잡아도 이 항목들은 그대로 유효합니다"**라고 말한다 —
앵커 정확도와 무관하게 항상 맞는 유일한 화면이다.

### 통로에는 작업 전 점검 의무가 없다 (전수 확인)

통로 그룹 10개 조문은 전부 구조 기준이고 시점 문구가 하나도 없다. 별표 3·별표 2에도 통로는 없다.
가장 가까운 것은 제42조제2항제7호("이동식 사다리 **사용 전** 점검")인데 추락 방지 절이라 통로가 아니다.
**통로는 만들 때 구조를 지키는 것이지 쓰기 전에 점검하는 것이 아니다.**

---

## 운영 메모

- `article_phases.json` 재생성: `python data-team/01-parsing/rule-appendices/build_article_phases.py`
  (LLM 재호출 없음 — `article_phase_llm/`의 원본을 결정론으로 병합만 한다)
- 흐름 재생성 후 **`serving-team/08-app/backend/app/data/trackA/`로 동기화 필수**.
  백엔드는 흐름을 프로세스 캐시에 담으므로 **재시작해야** 반영된다
- 검수 뷰어: `python data-team/01-parsing/rule-appendices/build_flow_viewer.py` (HTML 미추적)
- 저장된 분석 기록의 `work_flow`는 **저장 시점 데이터**다. 화면에서 새 흐름을 보려면
  앵커 정정(대안 칩/검색)으로 실시간 조회를 태우거나 새로 분석해야 한다

## 남은 일

1. **라벨 검수 0/2,295** — ⭐ 다음 작업. 뷰어 `http://localhost:8920/flow_review_viewer.html`
   (`.claude/launch.json`의 `flow-review-viewer`, 8920). **'의심스러운 것만'** 버튼이 116건만 남긴다.
   순서: 3인이 갈린 2건(제134·338조 — 제52조는 이미 사람이 뒤집었다) → 이름 매칭 94건 → 법령 신규
   ⚠ **CSV 반영 경로가 아직 없다.** 지금은 `human_review.json`에 손으로 넣는 것뿐이다.
     검수 CSV → `human_review.json` 변환 스크립트를 만들어야 한다
2. **곤돌라가 자동 앵커가 안 된다** — 전용 조문이 제160조 하나뿐이고 observable='no'라
   `nobs >= 1` 필터에 걸린다. 데이터는 맞는데 필터 기준("그룹 조문이 관찰 가능한가")이
   앵커의 질문("기인물이 사진에 보이는가")과 어긋난다. 안전검사 대상이고 흐름 41건이 있다.
   고치면 카탈로그 106종 → RESOLVE 재실행·재측정이 또 따라온다
3. **추락·붕괴는 cross_cutting에 남겼다** — 재해 형태이지 기인물·장소·환경이 아니고 흐름도 얇다
   (추락 전2 중8 / 붕괴 중3). 다르게 볼 여지가 있어 기록해 둔다
4. **「안전검사대상기계등의 규격 및 형식별 적용범위」 고시** — 혼합기·파쇄기 제외 범위의 유일한 출처.
   그리고 **시행규칙 제125조 면제 11개 반영** 여부 결정(화면에서 면제 가능성이 숨는다)
5. **얇은 흐름 보강** — 종료 35%·정기 25%. 가이드 연결은 15종뿐
6. **빈 흐름 9종 · 1칸 16종** — 대부분 통칙이라 §11에서 '부적격'으로 표시했지만
   앵커 카탈로그에서 빼는 것은 아직 안 했다(RESOLVE가 여전히 고를 수 있다)
7. **`gimulmul_index` 카탈로그 좌표 뭉갬** — 3그룹·37조문. 흐름 조립에서만 우회했다
8. **주 기인물 선별** — 정정 로그가 쌓이면 실사용 오인식률로 측정 가능
