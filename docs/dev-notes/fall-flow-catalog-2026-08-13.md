# 추락·붕괴(절1·절2) 카탈로그 편입 — 개구부류 사진의 흐름 누락 수리 (2026-08-13)

> 커밋 ee36efe(P1 데이터) · 55c7449(P2 계측) · fe8d54f(P3 문구). prod 배포·실증 완료.
> 지표 정본 = [evaluation-baseline.md](../status/evaluation-baseline.md) 앵커 절(2026-08-13).

## 문제와 원인

사용자 제기: 추락·붕괴 조문이 기본 안전수칙(basics)에만 있어 **기인물이 개구부인 사진에서
흐름이 통째로 누락**. prod 실증(경북환경 계단 사진): Vision은 정확(추락 HIGH 0.95·난간 부재
0.95·FALL 1.0)한데 앵커가 '잔재물 등의 조치기준'으로 뭉개져 흐름=가스·환기·잔재물 3건,
'지금 당장' 빈 목록, 추락 조문 0건 노출.

원인은 phase 라벨이 아니라 **카탈로그 배제**: 절1 추락(제42~49)·절2 붕괴(제50~53) 흐름 row는
완성돼 있었으나 `gimulmul_index.cross_cutting`에 묶여 RESOLVE 카탈로그에서 제외
(`cue_article_service.py:165`) — 사진 경로로는 앵커 도달이 원리적으로 불가. alias는 이미 절1로
배선돼 있었다(슬라브단부 121·바닥개구부 100·계단 73). '장7 비계' 선례(2026-08-02)와 동일 구조.

## 결정 (사용자, 2026-08-13)

- 설계 대안 A(서빙 결정론 병기·앵커 불변) vs B(카탈로그 편입·원인 수리) vs C(A 선배포+B 계측
  후결정) 중 **B 채택**. 알려진 한계 수용: 장비 앵커가 정확한 사진(비계·사다리)의 흐름에
  추락 대체 체인(제42~44)은 여전히 안 오른다 — 후속 검토(하단).
- Track A "장소 앵커 신설 안 함"(2026-08-12)은 **"기존 편제 그룹의 카탈로그 복귀는 허용"으로
  축소 변경**. '개구부' group_key 신설은 계속 금지(편제 좌표와 1:1이 아니어서 채점 역산이
  깨지고, 검수·alias 자산을 버리게 됨).
- 판별단서 라벨(문안 확정): 절1 `개구부ㆍ바닥 슬라브 개방 단부ㆍ계단 상부 개방 단부ㆍ지붕 위
  작업ㆍ난간 없는 추락 위험 장소` / 절2 `토사 사면ㆍ흙막이 없는 수직 절토면ㆍ구축물 균열
  기울어짐 붕괴 위험 장소`. '추락 방호망 안전대 부착설비' 조각은 **기각** — 충돌 대조표에서
  고소작업대('고소')·밀폐공간('안전대 등')·비계 어휘와 겹쳐 절도 리스크만 늘고 장소 판별
  기여 없음. '계단'은 공백 융합(함정 4호)·'굴착' 토큰 금지(굴착기·노천굴착·별표4 절도).

## 구현 (P1 — 생성 스크립트 체인, diff 게이트 3종 PASS)

1. `build_gimulmul_index.py`: CROSS_CUTTING_JANG에서 장6 제거 + GIM_ENRICH 라벨 2건 →
   **gimulmul diff: cross_cutting 5→3 + 라벨 2건 외 0**.
2. `build_flow_slice_all.py` 재실행 → **flow diff: 절1·절2 row의 name 필드만**(이름매칭 오부착 0).
3. `build_always_applicable.py`: TOPICS에서 '추락·붕괴 방지' 제거(승격) → 45→33건·5→4주제.
   ⚠ 잔여 diff = 남은 4주제의 evidence 인용 채움 — **B-B(2026-08-12) evidence fallback 재생성
   지연분**(always가 그 후 재빌드된 적 없었음). 이번 변경과 무관한 파이프라인 지연, 순수 개선.
4. `flow_service.py`: PLACE_TOPIC_BY_CODE에서 UNSTABLE_EDGE 제거·PLACE_DEFAULT_TOPICS 2주제
   (사라진 주제명 참조 = measure 러너 assert·빈 카드 지뢰). ★ **Track A(judge) 재개 시
   place_gold 재라벨이 선행 의무** — 개구부 3행 expect place→anchor(절1), 그 전에는
   measure_place_routing이 주제명 assert로 실패한다(의도된 실패 — 지뢰가 아니라 알람).
5. SR 확인: 제42~53 전부 sr_article_mapping 존재, 제42~49 FALL 히트. rtype 대부분
   PHYSICAL_PROTECTION → actable=False('계획 조치'), 제47조(EMERGENCY_RESPONSE)만 immediate.

## 계측 (P2 — 사전등록 게이트 전건 PASS, `measure_fall_catalog_gates.py`)

재추첨 = rerun_resolve_cache 129장(실패 0, 구캐시 `.61f43ddf627c.json` 보관 — RANK A/B 불훼손).
LLM 비용은 RESOLVE 129콜 1회뿐(라벨 반복 예산 2~3회 잡았으나 1회에 통과).

| 게이트 | 결과 |
|---|---|
| G1 기존 51장 히트→미스 (exact·flow·채점이탈) | **0 · 0 · 0** |
| G-FIRST 기존 exact-hit의 첫 키 gold 밖 절도 | **0** (변경 10장 전부 gold 안 — 라벨 표기차 위주) |
| G2 정답 그룹 4후보 밖 밀림 | **0** |
| 신규 채점 46장 (보고) | **전부 exact 히트** (개구부·단부·계단·지붕·경북환경 포함) |
| G5 스모크 | 절1 흐름 PRECHECK2/EXEC8 + '지금 당장' 6건 FALL 전부 히트 |

**exact 0.784/51장 → 0.959/97장 · 흐름 유효율 0.961 → 0.990 · 완전 오인식 4장(4.1%)**.
모수가 달라 집계 직접 비교는 불가 — 연속성은 per-photo G1이 보장.
G3(place_gold 개구부 3장) 원정의는 **미실행**: place_gold는 별도 사진 세트(Track A 스테이지
캐시)라 gold129 재추첨에 없다. 실질은 gold129 신규 채점(칸플랜트 개구부·천안테크노 단부 등
전부 exact)으로 입증 — place_gold 3장 재추첨은 Track A 재개 시 재라벨과 함께.

## prod 실증 (P3 — 배포 후 경북환경 사진 재분석)

| | 전 (2026-08-12 기록) | 후 (2026-08-13 재분석) |
|---|---|---|
| 앵커 | 장10 잔재물 등의 조치기준 (환경) | **절1 추락에 의한 위험 방지 (장소)** — 잔재물은 대안으로 |
| 흐름 | EXEC 3건(제83·84·85) + 5칸 공백 | **PRECHECK 2(제42·44) + EXEC 8(제42~49)** |
| 지금 당장 | 빈 목록 | **6건 — 제43조 '개구부 등 추락 위험장소 방호 조치'가 AI 대조 matched로 최상단** |

무회귀 스팟체크: 지게차 흐름(6/12/9/35/8/7칸·immediate 3건) 불변, /basics 4주제 33건.
화면 문구: kind='장소'면 "확인된 기인물"→"확인된 위험 장소", _empty_reason 명사 분기.
배포: 백엔드+프론트 이미지 재빌드(trackA JSON은 백엔드 이미지 내장) → update-ohs-code.sh →
이미지 ID 로컬 대조 일치. 롤백 = 생성물 git revert + 재생성 + 재배포(플래그 없음 — 카탈로그는
데이터).

## 남은 것 / 후속 후보

- **장3 통로 편입 검토**(계단 제26~30은 여전히 basics 전용) — 절1 편입 후 prod 정정 로그·계단
  사진 실측으로 필요성 판단.
- **장비 앵커 사진의 추락 체인**(기각된 설계 A의 병기 아이디어 재검토 후보) — 비계 사진 흐름에
  제42~44 대체 체인이 안 오르는 공백.
- **SR rtype 재검토**(data-team): 제47조 '수상·선박 구명장구'가 EMERGENCY_RESPONSE라 일반
  추락 사진에서 immediate 1순위로 뜨는 어색함. 제43조(덮개·난간 설치)의 planned 표기는
  규칙상 일관이나 사용자 체감 검토 여지.
- **Track A 재개 선행 의무**: place_gold 개구부 3행 재라벨(place→anchor 절1) + 게이트 재등록.
  현재 measure_place_routing은 PLACE_DEFAULT_TOPICS 축소로 assert 실패한다(의도).
- 경북환경 재분석의 alternates가 1개(잔재물)뿐 — 성형기(기계) 후보가 안 남는 문제는 이번 범위
  밖(RESOLVE 후보 다양성, 기존 관찰).
