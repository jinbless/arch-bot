# RANK A/B — cue-pool union 후보확장의 최종 랭킹 영향 (2026-07-29)

> **한 줄**: cue-pool union 후보확장은 최종 랭킹을 **해치지 않는다**(P@1 비열등 확정). 그러나 이 설계는 **이득을 검출할 검정력이 없으므로 "정확도가 올랐다"고 쓸 수 없다.** 확정 명제 = **"비열등 + 롱테일 도달범위 확대"**.
> 정본 수치: [../status/evaluation-baseline.md](../status/evaluation-baseline.md) 최상단. 선행: [cuepool-candidate-ceiling-ab-2026-07-13.md](cuepool-candidate-ceiling-ab-2026-07-13.md).

## 왜 이 측정을 했나
후보천장 A/B에서 union이 cand_any 84.5%→93.0%를 달성했으나 **천장 ≠ 최종정확도**. 후보가 평균 30.5→46.1개(+15.6)로 늘면 distractor가 랭킹을 해칠 수 있다. 이 실험이 그 판정이다.

## 설계 (사전등록 — 실행 전 확정)
paired 3-arm, 같은 사진·같은 Vision 캐시·같은 RESOLVE 결과 공유(교란 제거).

| arm | 후보 | 프롬프트 |
|---|---|---|
| A base_plain | baseline(gimulmul 기인물 앵커) | RANK_SYS(태그중립화) |
| B union_plain | baseline ∪ cue(entry+flow) | 동일 → **A와 차이 = 후보집합뿐** |
| C union_expert | union | + 감독관 SSOT rank_hint → **B와 차이 = 프롬프트뿐** |

- **주지표 = A→B의 P@1** 하나. 사전지정 비열등 마진 **−0.05**. 판정 사다리: CI하한>0 `gain` / >−0.05 `non_inferior` / CI상한<0 `harm` / else `inconclusive`.
- reps 4(짝수 — 홀수 rep은 후보 제시 **역순**으로 counterbalancing), 페어드 부트스트랩 CI.
- 유효성 게이트 G1(실패 0)·G2(|order_sensitivity|≤0.05)·G3(천장 재현)·G4(상수랭커 초과). **전부 PASS.**

### 실행 전 적대적 감사에서 잡은 치명 결함 (전부 실행 전 수정)
API 비용을 쓰기 전 5개 렌즈 감사 → 8건 CONFIRMED, 전부 실행 차단이었다:
1. **순서 편향** — gold 162개 중 88개(54.3%)가 [횡단] 블록인데 초안 정렬이 arm B에서만 그 블록을 통째로 아래로 밀었다(중앙 16→26). "A/B 차이=후보집합뿐"이 거짓이었음.
2. **태그 특혜** — RANK_SYS의 "[기인물] 표시 조 우선"이 arm B의 이득 원천을 100% 배제.
3. arm C **누출**(gold 노출 후 작성된 힌트), 4. RANK 실패 무음 소실, 5. CROSS 상수의 gold 커버, 6~8. 지표·검정력.

→ 수정 원칙 **"프롬프트는 후보 출처를 텍스트로도 순서로도 드러내지 않는다"**: 태그 미렌더 + 조번호 단일 정렬 + counterbalancing.

### ★검정력 — 이 실험의 성격
이득경로(headroom = A후보가 gold를 못 담고 B는 담은 사진)는 **12장뿐**. P@1 이론상한 **+0.093**, 실현 gross +0.050 < **MDE80 0.066**.
→ **이득은 원리적으로 검출 불가, 해악만 검출 가능한 설계.** Δ≈0은 "이득 없음"이 아니라 "이 표본으로는 볼 수 없음".

## 결과

| arm | P@1 | Hit@3 | Hit@5 | Hit@10 | R@5 | MRR | 천장 | 평균후보 |
|---|---|---|---|---|---|---|---|---|
| A base_plain | 0.432 | 0.609 | 0.680 | 0.725 | 0.611 | 0.531 | 0.837 | 30.5 |
| **B union_plain** | **0.438** | **0.655** | **0.713** | **0.779** | **0.654** | **0.557** | **0.930** | 46.1 |
| C union_expert ⚠ | 0.444 | 0.638 | 0.700 | 0.754 | 0.645 | 0.551 | 0.930 | 46.1 |

**주지표 A→B P@1 Δ+0.006 CI[−0.039,+0.052] → `non_inferior`** (사전등록 케이스 b1).
부지표 전부 양의 방향이나 **CI가 0을 포함**. **0을 배제한 유일 지표 = Hit@10 Δ+0.054 CI[+0.002,+0.111]**.

### 이득과 손실의 완전 분리 (핵심 발견)
| 지표 | headroom(12장) | 비headroom(117장) | 전체 |
|---|---|---|---|
| P@1 | 0.000 → **0.542** | 0.4765 → 0.4274 (**−0.049**, CI[−0.079,−0.024] **0 배제**) | +0.006 |
| Hit@3 | 0.000 → 0.729 | 0.671 → 0.647 (−0.024) | +0.047 |
| Hit@5 | 0.000 → 0.812 | 0.750 → 0.703 (−0.047) | +0.033 |

**B승 7장 = 100% headroom · A승 14장 = 100% 비headroom** (불일치 14쌍이 14-0, 부호검정 p=0.00012).
→ 비headroom 손실은 **통계적으로 실재**한다. 순효과가 0에 가까운 것은 이득이 손실을 겨우 상쇄해서다.

### 진짜 층화축은 headroom이 아니라 CROSS16
headroom 12장은 **12/12 전부 `gold ∩ CROSS16 = ∅`**.

| 층 | n | ΔP@1 | ΔHit@3 | ΔHit@5 |
|---|---|---|---|---|
| gold ∩ CROSS16 = ∅ | 56 | **+0.080** | **+0.134** | **+0.143** |
| gold ∩ CROSS16 ≠ ∅ | 73 | −0.051 | −0.021 | −0.051 |

**cue-pool은 횡단 일반의무 밖 롱테일(제68조·제122조·제87조·제302조 등)에서 벌고, 이미 잘 맞히던 횡단 사진에서 순위를 흔들어 잃는다.**

### 다음 병목은 후보생성이 아니라 랭킹 변별
- 천장 +12.0장(108→120장) 대비 **Hit@5 실현 +4.3장 = 실현율 35%**. 미실현 갭이 A 20.2장 → **B 28.0장으로 오히려 확대**.
- 최대 단일 손실원 = **추락 형제조문 변별**: gold 제43조 39장(gold y의 24.1%) P@1 A 0.705 → B 0.635 → C 0.481.
  gold 제13조 15장은 **3 arm 모두 top1 정답 0장**(제13조는 CROSS 상수라 129/129 후보에 이미 있음 → 도달성이 아니라 변별 문제).

### arm C — 감독관 프롬프트 힌트는 실패
gold 노출 후 작성(홀드아웃 없음, 커밋 `9c322b4`)이라 **상한추정**인데, 그 상한에서조차 순이득 ≈ 0
(headroom P@1 +0.042·Hit@3 +0.062 = **+0.75장** / 비headroom +0.002·−0.026 = **−3.00장** → 순 **−2.25장**).
실제로 힌트가 한 일은 의도한 제13조 조건부 분기가 **한 번도 실행되지 않고**(gold=제43조인데 top1=제13조인 사례 2→2, 감소 0) **제43조→제56조 오스왑**을 유발한 것.
→ **프롬프트 힌트 노선 폐기.** 감독관 지식(SSOT)이 틀린 게 아니라 **자유텍스트 힌트가 전달 수단으로 부적합**. 후보생성/온톨로지 제약·결정론 후처리로만 형식화한다.

## 판정 및 채택
**조건부 채택 — 연구트랙 후보생성 기본값을 union으로 전환.**
근거: P@1 비열등 확정 · 다건 지표 전부 양의 방향 · Hit@3 78.5→84.5장(+6.0장) · 4/4 rep 전원일치 top-3 신규전환 8장 대 이탈 0장 · 다건 제시 UX 확인(프론트 최대 5건, 페널티 경로당 4건, **단일 제시 경로 없음**).

| 조건 | 내용 |
|---|---|
| **1 (차단)** | 랭커 출력의 **후보-밖 코드 필터 구현**. 환각이 A 16건 → **B 24건(+50%)**. 필터 없이 배선하면 미측정 해악이 서빙에 나감 |
| 2 | 채택 범위 = 연구트랙. 서빙 backend에 cue-pool 참조 **0건**(grep) — 배포 정확도 개선 아님 |
| 3 | 기록 문구를 "비열등 + 롱테일 도달범위 확대"로 고정 |
| **4 (보류)** | **cue-pool pruning 실행 금지** — 같은 129장에서 역산한 순환 결과. LOPO 검증 시 천장 0.930→0.899, headroom 12장 중 4장 상실. 별칭 오탐 정리만 무손실(단 랭킹 효과 0으로 기대) |

## 반증된 주장 (1차 분석이 냈으나 적대적 검증에서 무너진 것)
과거 실패(합성 76% wash, facet 1.9% 오진)와 **같은 함정**이라 명시적으로 남긴다.

| 주장 | 판정 |
|---|---|
| "제4조가 최대 노이즈원, 끊으면 회수" | **반증** — 오답슬롯 241건 중 7건(9위, 2.9%), 제거 시뮬 기대이득 ≈0 또는 음수 |
| "비계 제58·59·60조가 손해" | **반증** — A승 14장에서 top1 차지 **0건**. 비계 발화 31장은 오히려 순이득 ΔP@1 +0.065 |
| "cue 27조문 pruning하면 이득 보존·손실 감소" | **반증** — 순환. LOPO 시 headroom 4장(제21조·제4조의2·제16조·제302조, 전부 단독 공급) 상실 |
| "후보를 늘려도 희석 없음(dilution 기각)" | **반증** — 추가 0개 사진이 0/117장이라 계단효과 검정 불가. 0용량 조건이 곧 arm A이고 A가 +0.049로 이김 |
| "손실은 후보집합이 아니라 프롬프트 문제" | **반증** — A/B는 후보집합만 다름. 공유코드 재정렬도 후보확장의 인과적 결과 |
| "손실 귀속 22%/48%/30%" | **반증** — 일관 규칙으로 재현 불가(22/57/22 또는 17/26/57). rep0는 A승을 과소검출 |

## 오독 방지 (보고·인용 시 필수)
1. "cue-pool로 정확도 4.7%p 상승" ❌ → "**해치지 않았다(비열등)**. 이 설계는 이득 검출력이 없다" ✅
2. "Hit@3 유의 개선" ❌ (재부트스트랩 하한 −0.002) → "다건 지표는 양의 방향이나 CI가 0 포함. 0 배제는 Hit@10뿐" ✅
3. "천장 93% = 서빙 정확도 93%" ❌ → "천장은 상한. 실현율 35%, 미실현 21.7%p" ✅
4. **비율 대신 절대 사진수로 보고** (headroom 분모가 12장이라 %가 부풀려짐)
5. 천장 0.845(선행 절) vs 0.837(본 절) **혼용 금지**
6. docstring "이득경로 11장"은 사전등록 시점 추정 → 실측 12장. **이력을 남기고 소급 수정하지 않음**

---

## 후속 ① 형제조문 변별 진단 + 변별 프로브 (2026-07-29, 같은 날)

"랭킹 변별이 병목"이라는 위 결론을 파고든 결과, **문제가 재정의됐다.**

### 진단 — 측정이 40% 눈감고 있었다
arm B rep0 top1 129장 분해: 정답 57(44%) · 후보천장 밖 9(7%) · **큐레이터가 판정한 적 없는 코드 51(40%)** · 판정된 오답 12(9%).
→ **"오답 72장" 중 71%는 오답이 아니라 미판정.** 확정된 랭킹 실패는 12장뿐.
- gold는 **감독건 단위 인용목록을 사진별 투표용지로 복제**한 구조. 판정 코드 수 중앙값 3개.
- **제13조 vs 제30조 판정쌍 = 0** (제30조가 gold=제13조 15장의 투표용지에 한 번도 없음) → **이 분기는 현 데이터로 채점 원리적 불가.**
- 형제-대-형제 판정쌍 65개(34장), 클러스터 부트스트랩 **MDE80 ±0.23** → 형제 변별 개선은 **증명 자체가 불가능**.
- gold 제13조 15장 중 7장(46.7%)은 원본 사진상 **난간이 물리적으로 전무**(이미지 직접 확인 4장). `대산…계단.jpg`는 gold=제13조인데 난간 전무 → 감독관 규칙대로면 제30조이고 **3 arm 모두 제30조를 top1로 냈는데 오답 처리**됨 = 라벨 아티팩트.

### 발견된 배관 결함 3종 (사실이나 원인은 아니었음)
1. `scene_text()`가 `hazards[].location/description`을 버려 **Vision 산출 텍스트의 52.9%가 랭커에 미도달**(버려진 쪽에 추락방호망·안전대 14→46장, 개구부·단부 34→53장 등 변별어 집중)
2. `section`(장7 비계 / 장3 통로 / 장2 작업장)이 프롬프트에 **미렌더** — 최강 변별자가 전달된 적 없음
3. `fullText[:160]` 고정 절단이 제56조 "안전난간을 설치할 것"(461자)·제68조 최상부난간(215자)·제13조 정량기준(219자~)·제42조 추락방호망(189자)을 **전부 삭제**

### 변별 프로브 — 배관·렌더 가설 **기각**
후보생성·미판정 노이즈를 제거하고 **큐레이터가 y/n을 매긴 코드만**(평균 3개) 제시해 순수 변별력만 측정.
주지표 **JPA**(같은 사진의 (y,n) 쌍에서 y가 위에 온 비율), 99장·판정쌍 299·reps 4·사진 클러스터 부트스트랩.

| arm | JPA | CI95 | 형제 JPA(탐색적) |
|---|---|---|---|
| P0 현행 | **0.863** | [0.807, 0.909] | 0.719 |
| P1 +정보배관(위 결함1 수정) | 0.861 | [0.808, 0.907] | 0.719 |
| P2 +근거렌더(결함2·3 수정) | 0.866 | [0.814, 0.911] | 0.677 |

P0→P1 Δ**−0.002** CI[−0.016,+0.011] · P1→P2 Δ**+0.005** CI[−0.018,+0.028] → **셋 다 이득 없음.**

**결론(사전 선언한 해석 규칙대로)**:
- **H1-a(정보배관)·H3′(근거렌더)는 변별력 개선 요인으로 기각.** Vision 텍스트 52.9%를 복원해도, section·조문 호를 넣어도 움직이지 않는다.
- **랭커의 변별력은 원래 높다(86.3%).** 앞서 확인된 "top1이 판정된 코드인 경우 P@1 82~86%"와 일치.
- **∴ P@1 43.8%의 정체 = 변별 실패가 아니라, 후보 30~46개 중 큐레이터가 평가한 적 없는 코드를 40% 확률로 집는 것.** 그게 틀린 건지 맞는데 라벨이 없는 건지 **알 수 없다.**

⚠️ **"시스템이 86% 정확하다"는 뜻이 아니다.** 프로브는 후보를 3개로 줄여 어려운 부분을 제거한 설정이다. 전체 설정(46개 후보)에서의 배관·렌더 효과는 미검증이며 애초 상한 추정도 +6장 < MDE80 8.5장으로 검출 불가였다.

### 그래서 다음 병목은 "측정 복구"
**라벨 판정범위 확장**이 모든 후속 실험의 선행조건. 생성 완료:
- `serving-team/08-app/backend/scripts/build_label_round2.py` → `real-test-photo/label_photo/label_round2.csv`(gold와 동일 스키마) + `curation_viewer_r2.html`(블라인드 뷰어)
- 대상 **879쌍 · 129장**(사진당 평균 6.8, 최대 12) · 그중 형제조문 596건
- 블라인드 원칙 유지: 모델/arm 출처 비표기, 사진별 고정seed 후보 순서 무작위화, 1차 판정은 회색 참고(수정 불가), EXIF 회전 보정(`image-orientation:from-image` — 129장 중 71장이 회전 필요)

raw: `data-team/05-enrichment/runtime-artifacts/probe_discrimination.json` · 하네스 `scripts/probe_discrimination.py`

### 이 진단이 뒤집은 것 (기록)
- ❌ "형제조문 변별을 고치면 P@1이 오른다" → 형제 변별은 **현 데이터로 측정 불가**(MDE ±0.23), 제13↔제30은 채점 원리적 불가
- ❌ "배관 버그가 원인" → 버그는 실재하나 **변별력에 영향 없음**(프로브에서 기각)
- ❌ "제13조 gold는 부실 대용물(로프·띠) 케이스" → 원본 사진 확인 결과 **절반은 난간 전무 = 라벨 노이즈**

---

## 후속 ② 라벨 2차 검수 완료 — 측정 복구 확정 (2026-07-31)

사람(감독관) 검수 완료: 신규 1,051쌍 **100% 판정**(y 26 · n 1,025) + 1차 판정 수정 15건(n→y 11 · y→n 4).
병합 → `label_curation_gold_v2.csv` (y 162→**195건**, 원본 v1 불변·provenance 보존).

### 측정 능력 변화 (결정적)
| | v1 | **v2** |
|---|---|---|
| top3 판정 커버리지 | 30.6~31.3% | **100.0%** |
| top1 미판정 | 58~62장 | **0장** |
| 판정쌍(JPA) | 299 | **1,878** (6.3배) |
| 형제 판정쌍 | 65쌍/34장 (MDE ±0.23 측정불가) | **924쌍/76장** (측정 가능권) |
| 제13↔제30 판정쌍 | **0** (채점 원리적 불가) | **19장** |
| 제13↔제43 판정쌍 | 4장 | **45장** |

### v1/v2 나란히 재채점 (⚠rep0 top5만 — 4-rep 아님, 방향 확인용)
| arm | P@1 v1→v2 | Hit@3 | Hit@5 | top1 정답/오답/미판정 (v2) |
|---|---|---|---|---|
| A | 0.426→**0.504** | 0.612→0.667 | 0.674→0.721 | 65/64/**0** |
| B | 0.450→**0.527** | 0.651→0.721 | 0.729→0.783 | 68/61/**0** |
| C | 0.473→0.527 | 0.643→0.705 | 0.721→0.798 | 68/61/**0** |

- **라벨 변화만으로 전 arm +0.05~0.08** — 미판정이던 top1 ~60장 중 **~10장(17%)은 실제 정답**, ~50장은 오답 확정. "라벨 변화 이득"과 "코드 변화 이득"의 분리 보고 원칙대로 이 상승은 **모델 개선이 아님**.
- 신규 y 조문: **제13조 5·제24조 4(사다리 — 검수 중 사용자 발견)·제42조 4**·제4조 3·제43조 3·제44조 3 등.
- A→B 사진승패(v2·rep0): B승 8 · A승 5 (v1 4-rep에선 7:14였음 — B의 "오답" 일부가 실제 정답이었다는 방향. 확정은 4-rep 재실행에서).

### caveat
- v2 gold는 **모델 top3 기반 확장**이라 모델-노출 편향 있음(top3 밖 진짜 정답은 여전히 미판정 가능. 단 추락권은 형제 10종 전수로 완화).
- 위 표는 rep0(정순 제시)만 — order_sensitivity(A −0.043) 미상쇄. **정본 수치 갱신은 4-rep 재실행 후에만.**

→ 이제 유효해진 다음 실험: ① 4-rep 재실행(v2 gold·전체 랭킹 저장) + arm D promote-1 ② 변별 프로브 재실행(형제 JPA 이제 검출력 있음) ③ b-box 적용 재논의.

## 다음 실험 사전등록 초안
- **arm D promote-1 하이브리드**(신규): A 랭킹 기본 + union 랭커가 **baseline에 없던 신규 코드를 1위로 올린 경우에만** 그 1위 채택. rep0 계산상 P@1 0.4651(A 대비 +0.047 CI[+0.008,+0.093]) — **[미검증]** 발동 11장의 극소표본, rep0 top5만 사용. **확인 대상으로만 등록**.
- **제외**: 게이팅 arm(중첩CV 0.4343 vs 순열귀무 p≈0.34, 관측가능 대리지표 AUC ≤0.704로 전부 0.5 포함 → 기각). arm C(누출 — 재측정 시 신규 감독회차 홀드아웃에서만).
- **층화 사전등록·동결**: headroom 소속(LLM 0회로 결정) + CROSS16 내/외.
- **계층적 판정**: ① 비headroom Δ P@1 비열등(마진 **−0.02**, 현 −0.049보다 엄격) 통과 시에만 ② headroom Δ P@1 우월성(CI하한>0).
- **G5 신규 게이트**: `per_photo.ranked`가 전 rep 저장됐는지 확인(없으면 fusion·다수결 분석 금지). → **하네스에 이미 반영**(전체 랭킹·candidates·kind 저장).
- **표본**: n_headroom ≥ 24 필요(현 12). 자연비율 9.3%이므로 무작위 확장은 240장 — **사전선별 과대표집** 권장.
- 예상 호출: 129장×3arm×4rep = 1,548 (Vision·RESOLVE 캐시 재사용으로 추가비용 0).

## 재현
```bash
cd serving-team/08-app/backend && .venv/bin/python scripts/rank_ab_gold.py --reps 4 --arms A,B,C --workers 8
```
- Vision `intake_vision_gold.json`(한글 파일명 키) / RESOLVE `rank_ab_resolve_cache.json` 재사용 → Vision 재호출 없음
- gold `real-test-photo/label_photo/label_curation_gold.csv`(match=y, gitignore)
- 결과 `data-team/05-enrichment/runtime-artifacts/rank_ab_results.json` / `.md`

## 잔여 caveat
Vision 고정 캐시(gpt-4.1) 하의 결과 · RESOLVE 단일 draw · **음성 전용 사진 102장 제외 → 오탐 비용 미측정** · gold는 감독건 단위 위반인용을 사진에 귀속(사진별 판정 아님) · arm A ≠ 배포 파이프라인(태그·순서 중립화) → **절대값을 배포 성능 주장에 쓰지 말 것**.

관련: [[cuepool-ab-result]] · [[cue-centric-architecture]] · [photo-to-article-cwa-redesign](photo-to-article-cwa-redesign-2026-06-21.md).
