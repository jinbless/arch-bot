# 결과 화면 컨설팅 서사 재설계 — 진행 정본 (2026-08-09)

> 다음 세션 진입점. 목표: **UI 5단계 서사를 중심으로 알고리즘 흐름·체계도를 재편**한다.
> 화면이 먼저 확정됐고(아래 §1–2 구현 완료), 이제 백엔드 구조를 화면에 맞춘다.

## 1. 확정된 화면 구조 (구현 완료, 커밋 5cc5466)

```
① 사진에서 본 것    Vision 서술 통합(구 3패널). "서술이지 판정 아님" 명시
② 지금 당장         즉시조치 — 조문 기반으로 재배선됨(§2)
③ 원래 이렇게 관리   기인물 앵커 흐름 6칸 ★주인공. "사람 검수를 마친 자료"
④ 직접 확인할 자료   가이드(검증전 표기)·기본수칙 링크·안전검사 여부
⑤ 사고가 나면       벌칙 3경로 (⚠ 입력이 아직 ①갈래 — §4-a가 남은 이유)
(접힘) 분석 상세     정규화·SHE·위험요소별 가이드·표준절차·조문후보·근거추적
```

## 2. 즉시조치 재배선 (구현 완료, 커밋 9bfc80e)

- 모집단 = **앵커 흐름의 작업전·작업중 조문**(검수 2등급) — 가이드 CI 54,631 광역 매칭 폐기
  (실측 잡음: 지게차 사진에 38kV 개폐장치·연삭 CI가 나왔었다)
- 순위 = SR 신호 2개: `addresses_hazard ∩ 사진 사고형태` + `requirement_type` 행위형 우선
  (PROCEDURAL·PPE·비상대응 먼저 — 헤드가드 장착(EQUIPMENT_STANDARD)은 법정이라도 '당장'이 아니다)
- ⚠ **어휘 2세대 함정**: 사진 정규화(COLLISION) vs SR(STRUCK_BY) — `_facet_canon` 변환 없이
  교집합하면 항상 공집합으로 순위가 조용히 죽는다. 구현: `flow_service.statute_actions()`
- 앵커 없으면 기존 CI 경로 폴백. GPT 제안은 "법정 근거 아님" 표기로 보조 병기

## 3. 알고리즘 지형 (사용자와 확인 완료)

- 런타임 LLM 판단은 **RESOLVE 1회뿐**(닫힌 카탈로그 104종에서 기인물 선택, 앵커 0.647 계측)
- 흐름 = group_key → JSON 키 조회 (조문 연결은 빌드타임+검수로 확정, 런타임 매칭 없음)
- 즉시조치 = 흐름 부분집합 + SQL 코드 교집합 랭킹. **벡터는 어디에도 없음**(Chroma는 off인 조문후보만)
- 자산 등급: 1등급 원문류 / 2등급 흐름·앵커(검수·계측) / 3등급 SR·NS·CI·SHE(추출·연결 미검증)
  / 4등급 사진→SR 매핑(1.9~18% 실측 신기루)

## 4. 다음 작업 큐 — "UI 중심 체계 재편"

a. **⑤ 벌칙 조인**: 입력 조문을 ①갈래(미검증)가 아니라 **흐름 조문**에서 —
   `흐름 refs → penalty_rule_index(4,076)` 조인 하나. 서사 완성의 핵심 신규 작업
b. **legacy 갈래 계산 옵션화**: SHE 매칭·facet CI·표준절차는 접힘 전용인데 매 분석마다 계산된다.
   플래그/lazy로 내려 파이프라인을 5단계 산출 중심으로 단순화
c. **④ 가이드 소스도 앵커 경유로**: hazard_guide_relations(3등급) 대신 흐름의 가이드(권고)
   항목·앵커 그룹 가이드 활용 검토
d. **체계도·문서 갱신**: 4-layer 문서에 5단계 서사 구조 반영
e. **moellab 재배포**: 다듬기 일단락 시(경로 검증됨: build_bundle.sh(WSL)→test-restore→scp→update-ohs.sh)

## 5. 검증 방법

- 테스트 사진: `/tmp/forklift.jpg` (재원금속 지게차·작업자 혼재 사본) →
  `curl -POST /api/v1/analysis/image -F "image=@forklift.jpg" -H "Expect:"` (cd /tmp에서 상대경로 필수)
- 최근 검증된 analysis_id: 2e7af882-1a4a-4692-8619-47f9065ff0b3 (조문 즉시조치 6건 + AI 4건)
- 서버: ohs-backend-dev(8001)·ohs-frontend(5173, /ohs/ base) — launch.json
