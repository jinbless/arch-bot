# A0 계측 스프린트 결과 — 앵커 0.647의 해부 (2026-08-09)

> 계획 정본: `~/.claude/plans/silly-noodling-book.md` (승인됨). 이 문서는 A0-a·b·c 결과와
> 그에 따른 A1·A2 우선순위 재조정. 러너: `backend/scripts/measure_resolve_shuffle.py`
> (사전등록 게이트 포함), 산출: `runtime-artifacts/resolve_shuffle_report.json`.

## A0-b·c: 셔플·confidence (51장 × 6팔 = 306콜, 실패 0)

| 측정 | 값 | 판정 |
|---|---|---|
| v2 캐시 exact / 원순서 재실행 | 0.647 / 0.647 | 시간 드리프트 **0** |
| 순열 4종 exact | 0.647·0.647·0.647·0.627 | 순서에 안정 |
| 팔 간 exact 불일치 | **3.9%** (2장) | **G-SHUFFLE FAIL — 위치 편향은 병목 아님** |
| 다수결 이득 | +0.000 | '순서 무작위화+다수결' **기각** (사전등록대로) |
| 첫 키 불일치 | 29.4% | 후보 "순서"는 흔들림 — rows[0] 주 기인물 규칙의 리스크는 실재 (단 exact 무영향) |
| confidence <70 버킷 | 2장, 오류율 1.00 vs ≥70 버킷 0.327 | **G-CONF PASS** (CI [0.531, 0.796]) |

confidence 해석 주의: 게이트는 통과했지만 **모델이 과신한다** — 오답 18장 중 저확신은 2장뿐.
'저확신 확인 유도'는 달 가치가 있으나(잡히면 확실) 주 개선 수단이 아니다. 임계 재조정은
사후 튜닝이라 금지 — 다음 계측에서 재등록.

## A0-a: 오류 18건 선분류 (초안 — 사용자 확정 대상)

**흐름 포함 검사**(선택 그룹의 흐름이 gold 조문을 포함하는가): **0/18** — 이것이 결정적 단서.

| 분류 | 건수 | 사진 | 내용 |
|---|---|---|---|
| **S1. 비계 공통 절 미상속** (모델 잘못 아님) | **9** | #2,8,9,10,11,12,15,16,18 | 사진에 비계 실재, 모델이 구체 비계(강관·이동식)를 **맞게** 선택. gold 조문(작업발판·재료 제55~57조류)은 '비계 > 절1 재료 및 구조 등' 공통 절 소속인데 **구체 비계 그룹 흐름에 상속돼 있지 않아** 좌표 불일치·흐름 결손. 와이어로프(관7→형제 상속)와 동일 패턴 |
| **S2. 진짜 오인식 — 범주형** | 7 | #3,4,5(석면) #13,14(그라인더) #6(화기) #17(접지) | 석면: 비닐 밀폐·출입통제가 석면 해체 신호임을 못 연상(Vision 서술에 석면 부재). 그라인더: 카탈로그 라벨 '사출성형기 등'이 연삭기를 안 담는 것처럼 보임. 화기: 소화기·흡연 신호 누락. 접지: 전기 장 내 인접 절 혼동 |
| **S3. 경계/라벨 특성** | 2 | #1(타워크레인 마스트 관통 개구부) #7(병리실 — 감독 관심은 국소배기, 모델은 사진 속 실재 기인물 선택) | gold가 감독건 인용이라 갖는 관점 편향 — 모델 오류로 단정 불가 |

**도달 상한 재설정**: S1이 해소되면(상속 또는 채점 인정) 0.647 → **0.824**가 구조적으로 열린다.
프롬프트·검증으로 노릴 수 있는 것은 S2 7건 — "진짜 오인식 기준" 개선 여지는 최대 +0.14.

## 우선순위 재조정 (계측이 계획을 이렇게 바꿨다)

1. **⭐ 신규 최우선 — 비계 공통 절 상속 검토** (S1 9건 = 오류의 절반): 강관·이동식 등 구체
   비계 그룹에 '절1 재료 및 구조 등'(작업발판 제55~57조류) 상속. 모델 개선이 아니라
   **흐름 데이터 작업**(build_flow_slice_all 상속 규칙 + Sol 검수 + gold 재계측).
   ⚠ 적용범위 문언 확인 필수(버그 클래스 10회 — 제55~57조가 정말 모든 비계에 적용되는지).
   **사용자 결정 필요** (흐름 데이터 정책)
2. **판별 단서·카탈로그 서술 보강** (S2 조준): '사출성형기 등' 라벨에 연삭기·그라인더 명시,
   석면 그룹에 "출입구 비닐 밀폐·경고표지" 시각 단서 추가 → gold 51 재실행으로 검증
3. confidence 확인 유도(B1) — 저비용이니 진행하되 기대 낮게 (오답의 ~11%만 포착)
4. ~~순서 무작위화+다수결~~ 기각 (G-SHUFFLE FAIL)
5. **A2 propose-verify 기대 하향**: S2 오류는 대부분 "사진에 실재하는 다른 것을 고른" 유형
   (잔재물·전선은 실제로 보임) — "선택 후보가 사진에 있나" 검증으로는 안 잡힌다.
   판별 단서(2번) 이후 잔여 오류의 유형을 보고 재평가

## 재현

```bash
# 앵커 정확도 (LLM 0)
./.venv/bin/python scripts/measure_anchor_accuracy.py
# 셔플·confidence (자체 캐시 — 프롬프트·스키마·카탈로그 SHA 불일치 시 자동 재실행)
./.venv/bin/python scripts/measure_resolve_shuffle.py --workers 6
```
