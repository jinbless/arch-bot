# Workplans

This directory is for durable workplan summaries.

The active detailed workplan currently remains at root:

- `WORKPLAN_LLM_DOMAIN_GUARD.md`

When a workstream stabilizes, copy or condense it here instead of rewriting old run logs.
